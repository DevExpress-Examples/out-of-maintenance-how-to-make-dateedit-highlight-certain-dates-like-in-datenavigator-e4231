<Window 
    x:Class="CustomDateEditTest.MainWindow" 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:local="clr-namespace:CustomDateEditTest"
    Title="MainWindow" 
    Height="200" 
    Width="165"
    >
    <Window.Resources>
        <Style x:Key="specialDateBorderStyle" TargetType="{x:Type Border}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush EndPoint="0.5,0" StartPoint="0.5,1">
                        <GradientStop Color="#3A9869"/>
                        <GradientStop Color="#81E396" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="1,1,1,1"></Setter>
            <Setter Property="CornerRadius" Value="5"></Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <local:CustomDateEdit 
            SpecialBorderStyle="{StaticResource specialDateBorderStyle}" 
            SpecialDates="{Binding Dates, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}" 
            VerticalAlignment="Center"
            HorizontalAlignment="Left" 
            x:Name="customDateEdit1" 
            Width="150"/>
    </Grid>
</Window>
